const RandomBtn = ({ event }) => {
  return <div className="surprise-btn-container">
    <button className="btn surprise-btn" onClick={event}>surprise me</button>
  </div>
}

export default RandomBtn;